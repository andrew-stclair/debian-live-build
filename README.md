# Custom Debian live-build

Just a simple custom config

## Building

```shell
apt-get install live-build git
git pull https://github.com/andrew-stclair/debian-live-build.git
cd debian-live-build
sudo lb build
```
